{
  "name": "sprint-pulse-monorepo",
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev": "pnpm -r dev"
  },
  "devDependencies": {
    "@hono/zod-validator": "^0.7.6",
    "@supabase/supabase-js": "^2.94.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "hono": "^4.11.7"
  }
}
